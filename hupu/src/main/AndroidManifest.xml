<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cgy.hupu">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppThemeLight">
        <activity
            android:name=".module.splash.SplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeLaunch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".module.main.MainActivity" />
        <activity android:name=".module.messagelist.MessageActivity" />
        <activity android:name=".module.userprofile.UserProfileActivity" />
        <activity android:name=".module.login.LoginActivity" />
        <activity android:name=".module.account.AccountActivity" />
        <activity android:name=".module.setting.SettingActivity" />
        <activity android:name=".module.post.PostActivity" />
        <activity android:name=".module.browser.BrowserActivity"></activity>
    </application>

</manifest>